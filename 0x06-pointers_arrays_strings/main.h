#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>

char *_strcat(char *dest, char *src);
char *_strncat(char *dest, char *src, unsigned long n);
char *_strncpy(char *dest, char *src, unsigned long n);

#endif /* MAIN_H */
